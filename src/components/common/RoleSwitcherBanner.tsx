import React from 'react';

// The role switcher is now exclusively hosted in the Settings page with password verification (ciis)
export const RoleSwitcherBanner: React.FC = () => {
  return null;
};
